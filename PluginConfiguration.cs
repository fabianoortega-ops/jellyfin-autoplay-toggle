using MediaBrowser.Model.Plugins;

namespace JellyfinAutoPlayToggle
{
    /// <summary>
    /// Configurações persistentes do plugin (salvas em XML pelo Jellyfin).
    /// Por enquanto vazia — pode ser expandida no futuro, ex: lembrar o estado
    /// de autoplay por perfil ou adicionar um atalho de teclado.
    /// </summary>
    public class PluginConfiguration : BasePluginConfiguration
    {
    }
}
